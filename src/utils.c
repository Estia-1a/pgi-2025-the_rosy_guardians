#include <estia-image.h>
#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>

#include "utils.h"

pixelRGB * getPixel() {
    if (x>width || y>height || data=NULL) {
        printf("NULL");
    }
    else {
        return (pixelRGB *) &data[y*width*n+x] ;

    }

}






void setPixel()
 * 
 */

